import hyRequest from "../index"
import { UserMessage } from "./interface/type"

/* 登录接口 */
// export const getMenu = (data: UserMessage.Menu) =>
//     hyRequest.get({ url: "/login", data })

