使用的react，接口用node写的，整个系统是使用的模板进行修改，所以里面有些地方会比较乱<br/>

## what's this?

react+redux<br/>
react+redux+vite+antd

## 构建 Start

```javascript
pnpm install       // 安装依赖模块
pnpm run dev       // 运行开发环境
pnpm run build     // 正式打包，生成最终代码
pnpm run preview   // 本地运行正式打包后的最终代码
pnpm run prettier  // 一键格式化代码